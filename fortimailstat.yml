- name: Login to FortiMail and capture session
  uri:
    url: "https://20.20.20.4/api/v1/AdminLogin/"
    method: POST
    headers:
      Content-Type: "application/json"
    body: |
      {
        "username": "admin",
        "password": "admin"
      }
    body_format: json
    return_content: yes
    validate_certs: no
    status_code: 200
  register: login_result
