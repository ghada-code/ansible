---
- name: Set hostname via raw CLI commands
  raw: |
    config system global
    set hostname nouveau-nom
    end
  register: raw_result
  failed_when: false   # Always treat as success
  changed_when: "'set hostname' in raw_result.stdout or raw_result.rc == 0"
