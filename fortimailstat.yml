- hosts: fortimail1
  tasks:
  - name: Get system info
    fortimailconfig:
      action: "get"
      host:  "{{ host }}"
      username: "{{ username }}"
      password: "{{ password }}"
      resource: "SysStatusSysinfo"
